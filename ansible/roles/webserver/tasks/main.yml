---
- name: Install Apache
  yum:
    name: httpd
    state: present
- name: Enable HTTP and HTTPS
  service:
    name: httpd
    state: started
    enabled: yes

- name: Enable SSL module
  command: a2enmod ssl

- name: Restart Apache
  service:
    name: apache2
    state: restarted
    
